defmodule CheckPoint.WorkerReg do

end

# rpid = Process.whereis(Registry.WorkerReg)
#     name = {:via, Registry, {Registry.WorkerReg, initial[:name]}}
# [{wk, _} | _] = Registry.lookup(Registry.WorkerReg, "worker0")
